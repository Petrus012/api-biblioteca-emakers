version: '3.8'
services:
  db:
    image: postgres:16
    container_name: postgres-emakers
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
      POSTGRES_DB: biblioteca_db
    ports:
      - "5432:5432" 